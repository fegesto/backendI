<h1>Carrito nro: {{cart._id}}</h1>
    {{#if cart.products.length}}
    <ul>
        {{#each cart.products}}
            <li>
                <h2>{{this.product.title}}</h2>
                <p>Cantidad: {{this.quantity}}</p>
                <p>Precio: ${{this.product.price}}</p>
            </li>
        {{/each}}
    </ul>
    {{else}}
        <p>No hay productos en el carrito</p>
    {{/if}}